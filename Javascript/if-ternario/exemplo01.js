let idade = Number(prompt("Digite sua idade:"));

// Se idade >= 18 retorna "Maior de idade", senão retorna "Menor de idade".
let resultado = (idade >= 18) ? "Maior de idade" : "Menor de idade";
// ?=if e :=else

console.log(resultado);
alert(resultado);